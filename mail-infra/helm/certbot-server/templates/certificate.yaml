{{- if .Values.tls.enabled }}
{{- if not .Values.tls.domains }}
{{- fail "tls.domains is required — pass via --set 'tls.domains={your.domain.com}' at install/upgrade time" }}
{{- end }}
{{- range .Values.tls.domains }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ . | replace "." "-" }}-tls
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "pingmailer-certificates.labels" $ | nindent 4 }}
spec:
  secretName: {{ . | replace "." "-" }}-tls
  issuerRef:
    name: {{ $.Values.tls.issuer }}
    kind: ClusterIssuer
  commonName: {{ . }}
  dnsNames:
    - {{ . }}
    - "*.{{ . }}"
  renewBefore: {{ $.Values.tls.renewBefore | default "720h" }}
{{- end }}
{{- end }}
