apiVersion: v1
kind: Service
metadata:
  name: {{ include "opengovmail-raven-sasl.fullname" . }}
  labels:
    {{- include "opengovmail-raven-sasl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: sasl
      protocol: TCP
      name: sasl
  selector:
    {{- include "opengovmail-raven-sasl.selectorLabels" . | nindent 4 }}
