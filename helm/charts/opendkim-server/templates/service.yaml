apiVersion: v1
kind: Service
metadata:
  name: {{ include "opengovmail-opendkim.fullname" . }}
  labels:
    {{- include "opengovmail-opendkim.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: milter
      protocol: TCP
      name: milter
  selector:
    {{- include "opengovmail-opendkim.selectorLabels" . | nindent 4 }}
